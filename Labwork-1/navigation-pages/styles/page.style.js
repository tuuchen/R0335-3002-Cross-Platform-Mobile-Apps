import { StyleSheet } from 'react-native'

const page = StyleSheet.create({
  safeArea: {
    flex: 1,
  },
  container: {
    flex: 1,
    padding: 16,
  },
  view: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
})

export default page
